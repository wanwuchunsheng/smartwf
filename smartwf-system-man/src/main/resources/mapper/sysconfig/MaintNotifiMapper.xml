<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smartwf.sm.modules.sysconfig.dao.MaintNotifiDao" >

    <!-- 批量删除 -->
    <delete id="deleteMaintNotifiByIds" parameterType = "java.util.List">
	    delete 
	       from basic_maint notification  
	    where id in
	    <foreach collection="list" item="item" open="(" separator="," close=")">
	        #{item}
	    </foreach>
	</delete>
    
	<!-- 查询列表 -->
    <select id="selectMaintNotifiByPage" resultType="com.smartwf.sm.modules.sysconfig.vo.MaintNotifiVO">
        select 
			b.id,
			b.tenant_id,
			b.tenant_domain,
			b.wind_farm,
			b.route_address,
			b.secret_path,
			b.create_time,
			a.org_name as windFarmName
		 from  sys_organization a left join  basic_maint notification b on a.id =b.wind_farm where a.tenant_id=b.tenant_id and a.org_type=1
         <if test="bean.tenantId != null ">
              and a.tenant_id=#{bean.tenantId}
         </if>
         <if test="bean.tenantDomain != null ">
              and a.tenant_domain=#{bean.tenantDomain}
         </if>
    </select>
    
    <!-- 主键查询 -->
    <select id="selectMaintNotifiById" resultType="com.smartwf.sm.modules.sysconfig.vo.MaintNotifiVO">
        select 
			b.id,
			b.tenant_id,
			b.tenant_domain,
			b.wind_farm,
			b.route_address,
			b.secret_path,
			b.create_time,
			a.org_name as windFarmName
		 from  sys_organization a left join  basic_maint notification b on a.id =b.wind_farm where a.tenant_id=b.tenant_id and a.org_type=1
		 and b.id=#{bean.tenantId}
         <if test="bean.tenantId != null ">
              and a.tenant_id=#{bean.tenantId}
         </if>
         <if test="bean.tenantDomain != null ">
              and a.tenant_domain=#{bean.tenantDomain}
         </if>
    </select>
    
    
    
</mapper>